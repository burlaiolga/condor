namespace QueryLanguage.Models
{
    public enum Operator
    {
        eq, //Is equal
        ne, //Is not equal
        gt, // Greater than
        lt, //Less then
        ge, //Greater or equal
        le, //Less or equal
        like, //Like,
        and,
        or
    }
}
